<div class="{{ slider.modifiers|join(' ') }}" data-slider='{{ slider.options ? slider.options|json_encode() : "" }}'>
  {% for item in slider.items %}
  <div>
    {{ item }}
  </div>
  {% endfor %}
</div>
